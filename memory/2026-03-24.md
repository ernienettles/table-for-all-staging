# 2026-03-24 — Session Log

## Who Ernie Is (full context loaded today)

- 33 y/o, Jacksonville FL, Lambda School 2019 grad (React/Node)
- Never hired as dev — pivoting to AI/ML in 6–9 months
- Hardware: ASUS ROG Zephyrus G15 (RTX 3070, 1TB), Ubuntu full install
- Stack: Node/pnpm, OpenClaw (~/.openclaw + ~/openclaw-source), Lovable, Supabase
- AI/ML knowledge: XGBoost, KMeans, PCA, SMOTE, time series, FastAPI, PyTorch, CNNs, anomaly detection
- Interests: PC gaming (Steam/indie), Arizer Air Max 2, Alex Finn AI dashboard videos
- Tone: direct, no fluff, concrete copy-pasteable commands

## Persona Confirmed

- I'm **Jon** — Ernie's sarcastic CTO-butler-chief-hustler AI co-founder
- SOUL.md written: dry humor, direct, ships real products, roasts architecture then fixes it
- Ernie is building a **business with me as co-founder** — not employed, not contracted
- Revenue plan: websites → AI automation solutions for small businesses
- NOT selling courses or bringing in more people — full ownership model
- Competitive advantage: first mover in "AI as real business partner" space, documenting AI workflows internally
- Table for All: gentle/story-first tone, NOT guilt/manipulative

## Mission Control Dashboard

- Repo: `git@github.com:ernienettles/mission-control-hub.git`
- Lovable was design tool only — actual app runs locally
- Structure: React + TypeScript + Vite + shadcn/ui + Tailwind
- Built and working locally (`npm run dev` → port 8080)
- File changes: `src/lib/openclaw.ts` (WebSocket RPC client), `src/lib/store.ts` (state), `src/pages/Index.tsx` (data loading on mount)

## OpenClaw Gateway Auth — Critical Issue

**The core blocker: gateway WebSocket auth requires `operator.write` scope, and the gateway config token only gets `operator.read`.**

Details:
- Gateway runs via systemd service at `systemctl --user start openclaw-gateway` (PID separate from my shell — I must NOT kill it)
- Gateway config: `~/.openclaw/openclaw.json` — contains `gateway.auth.token`
- Device identity: `~/.openclaw/identity/device.json` and `device-auth.json`
- `openclaw devices list` shows two devices: `a96ef8e...` (Control UI) and `ac5403d9...` (gateway)
- `openclaw gateway probe --json` shows: "scope limited" = true for the config token
- The gateway itself stamps ALL tokens with only `operator.read` when they come from `gateway.auth.token`
- The CLI succeeds with `operator.write` because it uses device auth from `~/.openclaw/identity/`
- The browser WebSocket client cannot use device auth (requires RSA private key signing)
- The Control UI succeeds because it's a paired device that connects with its own device identity (device ID stored somewhere in the browser/connected app)
- Chat sessions send via `chat.send` RPC method (NOT `chat.completions.create`)
- The gateway uses non-standard WebSocket protocol: `{type: "req", id: "string", method, params}` not JSON-RPC 2.0
- Connect flow: receive `connect.challenge` event → send `connect` request with `auth.token`, `role`, `scopes[]` → get response with `ok: true/false`

**What was NOT resolved:**
- How to give the browser client `operator.write` scope without device auth
- Possible fix directions to investigate:
  1. `openclaw gateway token` subcommand — unknown if it can generate write-scoped tokens
  2. Setting `gateway.auth.mode: "none"` to bypass token auth entirely
  3. Finding where Control UI stores its device credentials to use that device identity
  4. `gateway.auth.deviceIdentityRequired: false` — existence and effect unknown
  5. Checking if `OPENCLAW_ALLOW_DEVICES=all` or similar env var helps

**Current token in gateway config (`~/.openclaw/openclaw.json`):**
`GPBywR7vz3YxF2wIWRshnp0mkL7dI9PrI_Dcdhw2h-o`

**Current device auth operator token:**
`GPBywR7vz3YxF2wIWRshnp0mkL7dI9PrI_Dcdhw2h-o` (same — from second rotation, should have full scopes in device-auth but gateway still limits to read)

## SSH/Git Setup

- SSH key generated at `~/.ssh/openclaw_github` (ed25519, `jon@openclaw`)
- Public key added to GitHub account
- Repo cloned: `/home/ernie/mission-control-hub`

## Sub-Agent Note

This session ran as a sub-agent. I need to write to `memory/YYYY-MM-DD.md` for persistence, not just in-session memory. Future sessions should be able to read this file and know what happened.

---

_Remember: the gateway auth issue is the main blocker. Everything else (openclaw.ts protocol, Vite proxy, store wiring) is done and committed._
